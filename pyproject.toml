[project]
name = "pathogen-intelligence-factory"
version = "9.0.0"
description = "Lean retrieval with separated 15-25 literature/news briefs, typed evidence analysis, Python-first free translation with LLM fallback, verified news bodies, GitHub Pages and WeChat package generation."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
