[project]
name = "pathogen-intelligence-factory"
version = "6.0.0"
description = "Generic pathogen terminology bootstrap, multi-source intelligence, bilingual evidence analysis, GitHub Pages and WeChat package generation."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
