[project]
name = "pathogen-intelligence-factory"
version = "7.0.0"
description = "Lean five-concept pathogen retrieval, Python-first relevance review, ranked Top-50 enrichment, bilingual evidence analysis, GitHub Pages and WeChat package generation."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
