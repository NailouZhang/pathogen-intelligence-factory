# v6 防漏检与零结果保护

1. 所有安全权威身份词逐词独立查询；
2. 限定缩写独立查询；
3. 分组 OR、分子、流行病学、临床和基因组查询补充；
4. 拼写、标点、连字符和分词 AND 兜底；
5. 每个数据库使用原生语法；
6. 全部分块都执行；
7. 高产成员不能通过共享分页额度挤掉罕见成员；
8. 身份查询返回但元数据稀疏的记录保留到内容补全之后；
9. 新闻先抓正文再最终拒绝；
10. PubMed 与 Europe PMC 7 天均为 0 时运行 90 天逐锚点 count-only 探针；
11. `anchor_coverage.json` 保存每个身份锚点在各来源的计划、执行和命中数；
12. `source_status.json` 区分真实零结果、接口失败、未配置凭据和待审核来源。

90 天探针只用于诊断，不会把旧文献混入 7 天日报。
