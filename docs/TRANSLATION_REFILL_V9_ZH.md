# v9 翻译补位与不轮空

翻译顺序保持免费路径优先：deep-translator Google → Python Google 兼容入口 → MyMemory → Gemini/Groq。长摘要按完整句分块，不截断。

v9 在批量 LLM 返回缺项时，对每个未完成标题、摘要和结构化分析字段再次调用完整单字段翻译链。译文必须通过中文比例、数字保留、术语恢复、非原文复制和占位符检查。

最终数量在翻译门禁后确定：候选池保留到翻译完成，`translation_ready=false` 的记录被排除后，从余下就绪候选重新排序补位，直到达到 50 或就绪池确实不足。
