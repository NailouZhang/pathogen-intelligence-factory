# 新闻正文身份硬门禁与错误页熔断 v11

本策略用于防止新闻标题正确、正文却来自导航栏、网站首页、侧栏热门文章、标准规范页或反爬拦截页。

## 三层门禁

1. **候选 URL 身份门禁**
   - 普通媒体页只接受同站 canonical/meta/JSON-LD URL；
   - 不再抓取普通页面的全部 `<a>` 或脚本 URL；
   - 聚合页外链必须匹配 publisher 域名或标题；
   - W3C/Schema/XML 文档站点预先拉黑。

2. **单候选正文身份门禁**
   - 结构质量通过后，使用 `relevance_assessment("", body, profile)`；
   - 标题被置空，确保正文自己必须出现目标病原体、成员病毒、疾病名或合格缩写；
   - 页面标题与原新闻严重不匹配、正文只有一条侧栏病原词时拒绝；
   - 拒绝后继续尝试下一个候选 URL 或浏览器渲染，不立即采用错误正文。

3. **运行级后置门禁和熔断**
   - `relevance_post_enrichment` 的 reject 结果实际剔除；
   - 同一 URL/正文哈希被不同标题复用时整组拉黑；
   - 同一事件的真实重复只保留质量最高的一条。

## 发布原则

未通过上述三层门禁的新闻不得进入翻译、五要素精读、本期总览、GitHub Pages 或微信公众号内容包。允许最终新闻数量少于目标数量，不能用无关长文本补足数量。
